# vim: ft=config

# Don't let ripgrep vomit really long lines to my terminal, and show a preview.
--max-columns=150
--max-columns-preview

# Add my 'web' type.
--type-add
web:*.{html,css,js}*

# Add my 'zshrc' type
--type-add=zshrc:*.zsh*
--type-add=bashrc:*.sh*
--type-add=automake:*makefile.in*

# Set the colors.
--colors=line:style:bold
# --colors
# match:fg:magenta
# --colors
# line:fg:green
# --colors
# path:fg:yellow

# Because who cares about case!?
--smart-case

# search hidden files and directories (dot files)
--hidden

# don't respect ignore files (.gitignore, .ignore, ...)
# --no-ignore

# Using glob patterns to include/exclude files or folders
--glob=!.git/*

